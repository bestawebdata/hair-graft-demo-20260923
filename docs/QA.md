# 表示・動作検証

2026年9月23日。

## 実施済み

- JavaScript構文、ローカルアセット参照、全ページ内リンク、デモの検索除外設定を確認。
- DOM実行環境で28項目を確認。動的セクション、医師名、リスク、症例予定枠、モバイルメニュー、必須入力、メール形式、電話形式、その他地域、確認・編集・完了・消去、地域選択引継ぎ、問い合わせ種別切替を含む。
- 確認画面で入力値をHTMLとして実行しないこと、完了時に氏名・連絡先等を消すこと、localStorage / sessionStorageを使わないことを確認。
- `file://` からの読み込みとフォームの構築を確認。ビルドやHTTPサーバーがなくても使用可能。
- WebMCPの登録と正常・不正入力はスタブによる機能確認のみ実施。対応ブラウザでの実機検証は未実施。
- ローカルHTTPでHTMLの200応答を確認。
- 主画像は1440×1080px、約111KBのWebPに最適化。外部フォント・CDN・解析・送信先を使用しない。

## 検証の限界

この環境では自動操作用ChromiumがmacOSの権限制約（MachPortRendezvousのPermission denied）で起動できませんでした。**スクリーンショットによる実画面検証、端末ごとのレイアウト検証は未完了です。** DOM検証はブラウザの見た目・描画の保証ではありません。

CSSにはPC・タブレット・700px以下のスマートフォン向けレイアウト、文字の折り返し、キーボードフォーカス、動きを減らす設定を実装しています。提示前に普段のChrome/Safariで、スマートフォン幅とPC幅の文字・フォーム・固定ボタンの表示を確認してください。

## 本公開前の追加確認

実端末Safari/Chrome、200%文字拡大、スクリーンリーダーでの読上げとフォーカス、正式な医療原稿・料金・施設情報の確認は本受付前の確認項目です。

## 公開後の確認

GitHub Pagesのビルド状態 `built`、公開URLと主要6ファイルのHTTP 200、ローカルファイルとのハッシュ一致を確認しました。GitHub Pages配下のパスからDOM実行を行い、治療説明とフォームが正常に構築されることも確認済みです。これは実ブラウザによるレイアウト検証を代替しません。

## 写真追加改訂の確認

上田医師の人物写真・診療風景・ISBI 2018写真を原資料と照合し、WebPに最適化しました。顔や治療内容は生成・加工していません。主画像は約83KB、公開ページで使う3画像の合計は約326KBです。症例の切り出し12点は左右の対応と画像範囲を目視確認し、公開用リポジトリの外に置いています。

改訂後にフォーム等28項目、公開版3画像、ローカル確認版15画像の参照と代替テキスト、症例欄の表示数（公開版0点、ローカル版12点）を確認しました。実ブラウザでの描画検証の制約は初版と同じです。
